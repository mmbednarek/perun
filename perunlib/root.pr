module root;

pub alias mem_size = u64;

pub struct StringRef {
    data: rawptr,
    size: u64,
}

pub struct InitInfo {
    invocation: StringRef,
    args:       StringRef[],
}

pub enum ExitStatus {
    Success,
    Failure,
}
